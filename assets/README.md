# Assets

`css/variables.css` stores the visual tokens. `js/` contains progressively extracted Vanilla JS modules. `icons/` stores independent SVG icons. Images continue to live in Supabase Storage so the static repository does not duplicate cloud media.
